// import { DialogAddAccount } from '../../utils/index';
export function HandleDialogSubmission({ openDialog, postMessage, db }) {
  return async function({
    team: { id: teamId },
    channel: { id: channel },
    user: { id: user },
    trigger_id: triggerId,
    actions
  }) {};
}
